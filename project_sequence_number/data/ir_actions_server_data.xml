<odoo>
    <record id="project_project_action_set_sequence_number" model="ir.actions.server">
        <field name="name">Set No.</field>
        <field name="model_id" ref="project.model_project_project"/>
        <field name="binding_model_id" ref="project.model_project_project"/>
        <field name="state">code</field>
        <field name="code">
            <![CDATA[
records = records.sorted(key=lambda r: r.id)
records.set_sequence_field_and_name()
            ]]>
        </field>
    </record>
    <record id="project_task_action_set_sequence_number" model="ir.actions.server">
        <field name="name">Set No.</field>
        <field name="model_id" ref="project.model_project_task"/>
        <field name="binding_model_id" ref="project.model_project_task"/>
        <field name="state">code</field>
        <field name="code">
            <![CDATA[
records = records.sorted(key=lambda r: r.id)
records.set_sequence_field_and_name()
            ]]>
        </field>
    </record>
</odoo>
