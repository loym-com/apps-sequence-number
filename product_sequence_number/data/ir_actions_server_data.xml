<odoo>
    <record id="product_template_action_set_sequence_number" model="ir.actions.server">
        <field name="name">Set No.</field>
        <field name="model_id" ref="product.model_product_template"/>
        <field name="binding_model_id" ref="product.model_product_template"/>
        <field name="state">code</field>
        <field name="code">
            <![CDATA[
records = records.sorted(key=lambda r: r.id)
records.set_sequence_field_and_name()
            ]]>
        </field>
    </record>
    <record id="product_product_action_set_sequence_number" model="ir.actions.server">
        <field name="name">Set No.</field>
        <field name="model_id" ref="product.model_product_product"/>
        <field name="binding_model_id" ref="product.model_product_product"/>
        <field name="state">code</field>
        <field name="code">
            <![CDATA[
records = records.sorted(key=lambda r: r.id)
records.set_sequence_field_and_name()
            ]]>
        </field>
    </record>
</odoo>
